/* Map existing live app state into SCREEN_MODELS.json. Never hard-code screenshot data. */
function buildHomeModelFromCurrentApp(){throw new Error('map existing renderLearn/current Home data here')}
function buildPracticeModelFromCurrentApp(){throw new Error('map existing Practice handlers/data here')}
function buildTalkModelFromCurrentApp(){throw new Error('map existing selected scenario/character registry data here')}
function buildStoreModelFromCurrentApp(){throw new Error('map existing Store/economy data here')}
function buildProfileModelFromCurrentApp(){throw new Error('map existing profile/settings/stats data here')}
