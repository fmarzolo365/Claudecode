# MARZI UI PRODUCTION V2 — PHASE 0

Use `3f05467` as technical baseline only after verifying it descends from clean Gold `940bdee`, contains no Wave 2 work, and Gold Call remains intact.

Create `claude/marzi-ui-production-v2`.

Read the entire pack, especially technical protocol, contracts, CSS, shell core, components, action adapters, art resolver and tests.

Implement ONLY:
1. V2 tokens/CSS architecture.
2. premium bottom-nav DOM/CSS.
3. preserve 5-tab routing, #learn alias, hash/back mechanics.
4. shell helper integration without rewriting show()/call history.
5. component helpers + action adapter boundary + art resolver.
6. Do NOT redesign root contents yet.
7. Gold Call unchanged.

Run syntax/focused nav checks + `python MARZI_UI_PRODUCTION_IMPLEMENTATION_PACK_V2/tools/validate_static_shell.py public/index.html`.
Capture one simple root at 390×844, 360×640, 200% nav, RTL nav, and one Gold Call proof.
STOP.

End exactly:
READY FOR MARZI UI V2 PHASE 0 REVIEW
